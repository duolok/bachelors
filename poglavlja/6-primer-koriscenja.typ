#import "../funkcije.typ": todo
= Пример коришћења
<коришћење>

У овом поглављу ће бити приказани сви имплементирани екрани корисничког интерфејса.

== Покретање апликације

Апликација се покреће из терминала на без додатних аргумената:

```bash
$ oxyd
```

При покретању, апликација инцијализује све колекторе и приказује почетни таб са најосновнијим метрикама:

#figure(image("../slike/home.png", width: auto),
  caption: [
    Почетна страница.
  ]
)<fig:pyflies>

== Навигација кроз табове

Имплементација користи навигацију која омогућава брз приступ различитим категоријама метрика.
Тренутно активан таб је истакнут другом бојом у header-у.

== Мониторинг процесора

CPU таб приказује детаљне информације о искоришћености процесора.

#figure(image("../slike/cpu.png", width: auto),
  caption: [
    Приказ странице за увид у искоришћење процесора.
  ]
)<fig:pyflies>

== Мониторинг меморије

Memory таб приказује детаљне информације о коришћењу RAM меморије и swap простора.

#figure(image("../slike/memory.png", width: auto),
  caption: [
    Приказ странице за увид у меморију.
  ]
)<fig:pyflies>

== Управљање процесима

Таб са процесима пружа детаљан увид у стање свих процеса оперативног система. Одавде је могуће филтрирање и руковање свим процесима.

#figure(image("../slike/process_table.png", width: auto),
  caption: [
    Приказ свих процес.
  ]
)<fig:pyflies>

#figure(image("../slike/search.png", width: auto),
  caption: [
    Филтрирање процеса.
  ]
)<fig:pyflies>

#figure(image("../slike/end.png", width: auto),
  caption: [
    Слање сигнала `end` процесу.
  ]
)<fig:pyflies>

== Мониторинг дискова

Disk таб приказује информације о искоришћености простора на диску.

#figure(image("../slike/disk.png", width: auto),
  caption: [
    Приказ странице за увид у стање диска.
  ]
)<fig:pyflies>


== Мониторинг мреже

`Network` таб приказује информације о тренутно доступним мрежним интерфејсима.

#figure(image("../slike/network.png", width: auto),
  caption: [
    Приказ странице за контролу над нотификацијама.
  ]
)<fig:pyflies>


== Нотификације и подешавања

Имплементација подржава подешавање и слање обавештења за процесор, меморију или диск уколико
метрика за њихово искоришћење пређе одређени проценат.

#figure(image("../slike/settings.png", width: auto),
  caption: [
    Приказ странице за увид у стање диска.
  ]
)<fig:pyflies>


#figure(image("../slike/notifications.png", width: auto),
  caption: [
    Приказ странице за увид у нотификације.
  ]
)<fig:pyflies>


== Контроле и пречице

Имплементација пружа интуитиван систем пречица са тастатуре који омогућава брзу навигацију и управљање свим аспектима 
апликације без потребе за мишем. 

Притиском на тастер `?`, отвара се специјални прозор са свим могућим опцијама за интеракцију са корисничким 
интерфејсом. Овај екран за помоћ је увек доступан из било ког таба и пружа брз преглед свих доступних команди.

#figure(
  image("../slike/help.png", width: 85%),
  caption: [
    Приказ екрана за помоћ са свим доступним пречицама и акцијама
  ]
) <fig:help-screen>

Списак свих акција је приказан у наредној табели. Пречице су организоване по категоријама за лакшу 
навигацију и коришћење.

#figure(
  table(
    columns: (auto, 1fr),
    align: (center, left),
    stroke: 0.5pt,
    fill: (col, row) => if row == 0 { rgb("#e8e8e8") } else { none },
    table.header(
      [*Тастер*], [*Акција*]
    ),
    
    table.cell(colspan: 2, fill: rgb("#f0f0f0"))[*Навигација између табова*],
    [`1` - `8`], [Директан прелазак на таб по броју (1 = Overview, 2 = CPU, итд.)],
    [`Tab`], [Прелазак на следећи таб (десно)],
    [`Shift+Tab`], [Прелазак на претходни таб (лево)],
    
    table.cell(colspan: 2, fill: rgb("#f0f0f0"))[*Навигација кроз листе*],
    [`↑` / `k`], [Помери селекцију на горњи елемент],
    [`↓` / `j`], [Помери селекцију на доњи елемент],
    
    table.cell(colspan: 2, fill: rgb("#f0f0f0"))[*Управљање процесима*],
    [`Shift+k`], [Угаси селектовани процес (SIGKILL - принудно гашење)],
    
    table.cell(colspan: 2, fill: rgb("#f0f0f0"))[*Филтрирање и претрага*],
    [`/`], [Активирај режим филтрирања процеса],
    [`Esc`], [Изађи из режима филтрирања и врати се на пун приказ],
    
    table.cell(colspan: 2, fill: rgb("#f0f0f0"))[*Сортирање*],
    [`p`], [Сортирај процесе по PID-у],
    [`n`], [Сортирај процесе по имену],
    [`c`], [Сортирај процесе по CPU искоришћености],
    [`m`], [Сортирај процесе по коришћењу меморије],
    
    table.cell(colspan: 2, fill: rgb("#f0f0f0"))[*Опште контроле*],
    [`?`], [Прикажи екран за помоћ са свим пречицама],
    [`q`], [Изађи из апликације],
    [`Ctrl+C`], [Принудни излазак из апликације],
  ),
  caption: [
    Комплетан преглед свих доступних пречица у Oxyd апликацији
  ]
) <tab:keyboard-shortcuts>
